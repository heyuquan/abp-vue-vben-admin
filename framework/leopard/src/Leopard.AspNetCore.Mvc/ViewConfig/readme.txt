参考  https://github.com/SpringLeee/ViewConfig
直接使用nuget，会出现奇怪的问题，所以直接把代码引入项目中。
直接引入会出现：如 在 Leopard.Host 中引入ViewConfig（0.0.5版本），编译和使用没有问题，但是当一个host项目引入Leopard.Host后，编译这个host项目，ViewConfig包会消失不见。（可能是不能引入预览版的东西吧）