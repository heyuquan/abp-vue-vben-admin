input {
	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "134.175.121.78:9200"
		user => "elastic"
		password => "changeme"
	}
}
