
1、为什么不把 Leopard 中的帮助类，移到 Leopard.Utils 项目中？？这样就可以在不使用abp的module情况下使用工具函数
   因为：
		拆的时候发现，utils里面的工具类有用到abp.core里面的一些公共函数。eg：Chcked.检查函数。   
		如果确实只是单独想使用工具函数，也依然可以引入 Leopard 程序集，而不使用abp的module。
		但不好的是Leopard也会把如abp.core这种基础类库引入，但不影响使用